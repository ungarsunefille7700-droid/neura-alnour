<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="NEURA AL-NOUR"
        android:supportsRtl="true"
        android:theme="@style/Theme.NeuraAlNour">

        <activity
            android:name="com.neuraalnour.app.LauncherActivity"
            android:exported="true"
            android:theme="@style/Theme.NeuraAlNour.Splash">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name="com.neuraalnour.app.TwaActivity"
            android:exported="false" />
    </application>
</manifest>
